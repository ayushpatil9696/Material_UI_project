import { makeStyles } from "@mui/material";

export default makeStyles(() => ({
    income: {
        borderBottom: '10px solid rgba(0, 255, 0, 0.5)',
    
    },
    expense:{
        boarderBottom: '10px solid rgba(225, 0, 0, 0.5)',
    }
}));